DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
CONCURRENCY=4
OUTPUT_DIR=output

# 可选：主 provider 连续报瞬时错误（429/5xx/连接失败）重试用尽后的备用 provider，
# 不填就只用上面的主 provider，行为不变
FALLBACK_API_KEY=
FALLBACK_BASE_URL=
FALLBACK_MODEL=
